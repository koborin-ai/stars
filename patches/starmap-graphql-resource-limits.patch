diff --git a/src/github/client.rs b/src/github/client.rs
index 3c9644c..e63d4ea 100644
--- a/src/github/client.rs
+++ b/src/github/client.rs
@@ -31,20 +31,12 @@ impl GitHubClient {
             let conn = data.viewer.lists;
 
             for raw_list in conn.nodes {
-                let mut star_list = StarList::from(raw_list.clone());
-
-                // Fetch remaining pages if items are paginated
-                if raw_list.items.page_info.has_next_page {
-                    let remaining = self
-                        .fetch_remaining_list_items(
-                            &raw_list.id,
-                            raw_list.items.page_info.end_cursor,
-                        )
-                        .await?;
-                    star_list.repositories.extend(remaining);
-                }
-
-                lists.push(star_list);
+                let repositories = self.fetch_all_list_items(&raw_list.id).await?;
+                lists.push(StarList {
+                    name: raw_list.name,
+                    description: raw_list.description,
+                    repositories,
+                });
             }
 
             if !conn.page_info.has_next_page {
@@ -102,14 +94,14 @@ impl GitHubClient {
         Ok(data.viewer.login)
     }
 
-    /// Fetch remaining pages of items within a list
-    async fn fetch_remaining_list_items(
+    /// Fetch all items within a list, paginating per list to stay under
+    /// GitHub GraphQL resource limits.
+    async fn fetch_all_list_items(
         &self,
         list_id: &str,
-        start_cursor: Option<String>,
     ) -> Result<Vec<Repository>> {
         let mut repos = Vec::new();
-        let mut cursor = start_cursor;
+        let mut cursor: Option<String> = None;
 
         loop {
             let query = build_list_items_query(list_id, &cursor);
@@ -231,24 +223,10 @@ fn build_lists_query(cursor: &Option<String>) -> String {
         r#"{{
   viewer {{
     lists(first: 100{after}) {{
-      totalCount
       nodes {{
         id
         name
         description
-        isPrivate
-        items(first: 100) {{
-          totalCount
-          nodes {{
-            ... on Repository {{
-              nameWithOwner
-              description
-              url
-              isPrivate
-            }}
-          }}
-          pageInfo {{ hasNextPage endCursor }}
-        }}
       }}
       pageInfo {{ hasNextPage endCursor }}
     }}
@@ -294,6 +272,7 @@ fn build_list_items_query(list_id: &str, cursor: &Option<String>) -> String {
             nameWithOwner
             description
             url
+            isPrivate
           }}
         }}
         pageInfo {{ hasNextPage endCursor }}
diff --git a/src/github/types.rs b/src/github/types.rs
index dc38786..16cb32b 100644
--- a/src/github/types.rs
+++ b/src/github/types.rs
@@ -40,10 +40,18 @@ pub struct ListsViewer {
 #[derive(Debug, Deserialize)]
 #[serde(rename_all = "camelCase")]
 pub struct ListsConnection {
-    pub nodes: Vec<RawUserList>,
+    pub nodes: Vec<RawUserListMeta>,
     pub page_info: PageInfo,
 }
 
+#[derive(Debug, Clone, Deserialize)]
+#[serde(rename_all = "camelCase")]
+pub struct RawUserListMeta {
+    pub id: String,
+    pub name: String,
+    pub description: Option<String>,
+}
+
 #[derive(Debug, Clone, Deserialize)]
 #[serde(rename_all = "camelCase")]
 pub struct RawUserList {
